---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptime_statuspage_user Resource - terraform-provider-uptime"
subcategory: ""
description: |-
  Status page user resource. Import using composite ID: terraform import uptime_statuspage_user.example statuspage_id:user_id
---

# uptime_statuspage_user (Resource)

Status page user resource. Import using composite ID: `terraform import uptime_statuspage_user.example statuspage_id:user_id`

## Example Usage

```terraform
# Create a status page first
resource "uptime_statuspage" "example" {
  name             = "My Service Status"
  visibility_level = "EXTERNAL_USERS"
}

# Add a user who can access the status page
resource "uptime_statuspage_user" "admin" {
  statuspage_id = uptime_statuspage.example.id
  email         = "admin@example.com"
  first_name    = "John"
  last_name     = "Admin"
  is_active     = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `email` (String)
- `first_name` (String)
- `is_active` (Boolean)
- `last_name` (String)
- `statuspage_id` (Number)

### Read-Only

- `id` (Number) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using composite ID: statuspage_id:user_id
terraform import uptime_statuspage_user.admin 123:456
```
